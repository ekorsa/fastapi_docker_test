apiVersion: 1

providers:
  - name: 'FastAPI Dashboards'
    orgId: 1
    folder: 'FastAPI'
    type: file
    disableDeletion: false
    editable: true
    options:
      # Grafana will scan this internal directory for JSON dashboard files
      path: /etc/grafana/provisioning/dashboards

